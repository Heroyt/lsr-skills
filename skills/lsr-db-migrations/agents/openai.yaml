interface:
  display_name: "LSR DB Migrations"
  short_description: "LSR app schema, indexes, FKs, and install migrations"
  default_prompt: "Use $lsr-db-migrations to add or review an LSR app database migration."
policy:
  allow_implicit_invocation: true
