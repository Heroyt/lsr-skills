interface:
  display_name: "LSR DB Migrations"
  short_description: "Modular LSR schema and migration guidance"
  default_prompt: "Use $lsr-db-migrations to add or review modular LSR database migrations."
policy:
  allow_implicit_invocation: true
